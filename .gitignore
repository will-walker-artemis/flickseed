node_modules
dist
.env
.env.local
.DS_Store
*.log

# Local-only notes — kept on disk, not shared
docs/

# Tool/editor caches
.vite
*.tsbuildinfo

# Python
__pycache__/
*.pyc
.venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Pipeline outputs that are large and regenerable
data/raw/

# Probe / exploration reports (regenerable, not the contract)
pipeline/reports/
